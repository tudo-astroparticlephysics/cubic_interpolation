@PACKAGE_INIT@

list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

find_package(Boost QUIET COMPONENTS filesystem serialization)
find_package(Eigen3)

include ("${CMAKE_CURRENT_LIST_DIR}/CubicInterpolationTargets.cmake")
